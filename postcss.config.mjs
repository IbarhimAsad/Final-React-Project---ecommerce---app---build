/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // REMOVED: ES module PostCSS config — using CommonJS `postcss.config.js` for Vite.
    // Archived copy: archive_next/postcss.config.mjs.bak (not present)
  },

export default config
